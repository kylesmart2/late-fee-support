// Fill this in once your Lambda + API Gateway (or Lambda Function URL) is deployed —
// see /lambda/README.md for the exact setup this expects. Left blank, the form will show
// a clear "not configured yet" error instead of failing silently.
window.LATE_FEE_SUPPORT_ENDPOINT = "";
